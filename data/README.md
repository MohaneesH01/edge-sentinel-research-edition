# Data

This directory stores telemetry and datasets.

- `edge_sentinel.db`: local SQLite database generated by the backend logger.
- `raw/`: raw captures and notes from hardware sessions.
- `processed/`: cleaned CSV files for analytics and ML.

Do not commit large generated datasets unless they are intentionally curated for publication or reproducibility.
